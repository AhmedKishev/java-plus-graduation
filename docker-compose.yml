services:


  stats-server:
    build: stat/stat-server
    image: stat-server
    container_name: stat-server
    restart: on-failure
    ports:
      - "9090:9090"
    depends_on:
      - db-stat
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-stat:5432/stat_db


  db-stat:
    image: postgres:alpine
    container_name: db-stat
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=stat_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=12345
    healthcheck:
      test: pg_isready -q -d $$POSTGRES_DB -U $$POSTGRES_USER
      timeout: 5s
      interval: 5s
      retries: 10


  db-user:
    image: postgres:alpine
    container_name: db-user
    ports:
      - "4432:5432"
    environment:
      - POSTGRES_DB=db-user
      - POSTGRES_USER=db-user
      - POSTGRES_PASSWORD=db-user
    healthcheck:
      test: pg_isready -q -d $$POSTGRES_DB -U $$POSTGRES_USER
      timeout: 5s
      interval: 5s
      retries: 10

  db-comment:
    image: postgres:alpine
    container_name: db-comment
    ports:
      - "3432:5432"
    environment:
      - POSTGRES_DB=db-comment
      - POSTGRES_USER=db-comment
      - POSTGRES_PASSWORD=db-comment
    healthcheck:
      test: pg_isready -q -d $$POSTGRES_DB -U $$POSTGRES_USER
      timeout: 5s
      interval: 5s
      retries: 10


  db-request:
    image: postgres:alpine
    container_name: db-request
    ports:
      - "2432:5432"
    environment:
      - POSTGRES_DB=db-request
      - POSTGRES_USER=db-request
      - POSTGRES_PASSWORD=db-request
    healthcheck:
      test: pg_isready -q -d $$POSTGRES_DB -U $$POSTGRES_USER
      timeout: 5s
      interval: 5s
      retries: 10

  db-analyzer:
    image: postgres:alpine
    container_name: db-analyzer
    ports:
      - "5444:5432"
    environment:
      - POSTGRES_DB=db-analyzer
      - POSTGRES_USER=db-analyzer
      - POSTGRES_PASSWORD=db-analyzer
    healthcheck:
      test: pg_isready -q -d $$POSTGRES_DB -U $$POSTGRES_USER
      timeout: 5s
      interval: 5s
      retries: 10


  db-event:
    image: postgres:alpine
    container_name: db-event
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_DB=db-event
      - POSTGRES_USER=db-event
      - POSTGRES_PASSWORD=db-event
    healthcheck:
      test: pg_isready -q -d $$POSTGRES_DB -U $$POSTGRES_USER
      timeout: 5s
      interval: 5s
      retries: 10


  kafka:
    image: confluentinc/confluent-local:7.4.3
    hostname: kafka
    container_name: kafka
    ports:
      - "9092:9092"   # для клиентов (localhost:9092)
      - "9101:9101"   # JMX
      - "29092:29092" # для внутренних клиентов (docker)
      - "8082:8082"
    restart: unless-stopped
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092'
      KAFKA_JMX_PORT: 9101
      KAFKA_JMX_HOSTNAME: localhost
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@kafka:29093'
      KAFKA_LISTENERS: 'PLAINTEXT://kafka:29092,CONTROLLER://kafka:29093,PLAINTEXT_HOST://0.0.0.0:9092'
      CLUSTER_ID: 'K0EA9p0yEe6MkAAAAkKsEg'

  kafka-init-topics:
    image: confluentinc/confluent-local:7.4.3
    container_name: kafka-init-topics
    depends_on:
      - kafka
    command: "bash -c \
                'kafka-topics --create --topic stats.user-actions.v1 \
                           --partitions 1 --replication-factor 1 --if-not-exists \
                           --bootstrap-server kafka:29092 && \
                kafka-topics --create --topic stats.events-similarity.v1 \
                           --partitions 1 --replication-factor 1 --if-not-exists \
                           --bootstrap-server kafka:29092'"
    init: true